<h2>Sign up</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name),:builder => BootstrapFormBuilder) do |f| %>
<%#= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <%= f.build_row f.label( :email), f.text_field( :email) %>
	<%= f.build_row f.label( :password), f.password_field(:password) %>
	<%= f.build_row f.label( :password_confirmation), f.password_field(:password_confirmation) %>
	<%= f.build_row nil, f.submit("Sign up")  %>
<% end %>

<%= render :partial => "devise/shared/links" %>
